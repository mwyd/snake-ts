import Config from './modules/interfaces/Config'

const config: Config = {
  debug: true,
  tileSize: 40,
  initLength: 3,
  tickrate: 10,
  snakeColor: 'green',
  fruitColor: 'red',
  boostColor: 'yellow',
  boostTimeout: 4 * 1000,
  reverseColor: 'purple',
  boardColor: 'white'
}

export default config